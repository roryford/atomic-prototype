{
  // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
  "version": "0.2.0",
  "configurations": [
    {
      "name": "ng serve",
      "type": "chrome",
      "request": "launch",
      "preLaunchTask": "npm: start",
      "url": "http://localhost:4200/"
    },
    {
      "name": "ng test",
      "type": "chrome",
      "request": "launch",
      "preLaunchTask": "npm: test",
      // Tests run via Angular CLI's built-in Vitest runner (no separate vitest.config needed).
      // There is no browser debug port — use `npm test` in the terminal to run tests.
      "url": "http://localhost:4200/"
    }
  ]
}
